# Q5

```sql
SELECT COUNT(DISTINCT UserID) FROM hits;
```

## Adversarial

COUNT DISTINCT UserID uses HashSet<i64> — 8 bytes/key; HyperLogLog would trade accuracy for RAM.

## This pass

Uses existing fast_agg global paths; document baseline.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 5.

## Pass 2 (faster)

Q5: global HashSet<i64> distinct — OK at 100k.
