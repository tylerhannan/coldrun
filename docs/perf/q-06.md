# Q6

```sql
SELECT COUNT(DISTINCT SearchPhrase) FROM hits;
```

## Adversarial

COUNT DISTINCT SearchPhrase is string-heavy; needs dedicated utf8 distinct or sketch.

## This pass

Uses existing fast_agg global paths; document baseline.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 6.
