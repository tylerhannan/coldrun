# Q15

```sql
SELECT SearchEngineID, SearchPhrase, COUNT(*) AS c FROM hits WHERE SearchPhrase <> '' GROUP BY SearchEngineID, SearchPhrase ORDER BY c DESC LIMIT 10;
```

## Adversarial

Two-key int+utf8 not fused; falls back to slow interpreter path.

## This pass

Int/int64 packed GROUP BY (group_int.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 15.

## Pass 2 (faster)

Q15: int+utf8 COUNT fused.
