# Q21

```sql
SELECT COUNT(*) FROM hits WHERE URL LIKE '%google%';
```

## Adversarial

LIKE %google% scans full URL column; trigram/ngram index absent.

## This pass

No code change this pass — adversarial notes only.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 21.

## Pass 2 (faster)

Q21: LIKE scan — memchr helps; no trigram index.
