# Q14

```sql
SELECT SearchPhrase, COUNT(DISTINCT UserID) AS u FROM hits WHERE SearchPhrase <> '' GROUP BY SearchPhrase ORDER BY u DESC LIMIT 10;
```

## Adversarial

COUNT DISTINCT per SearchPhrase group — hot path still HashSet per group.

## This pass

Utf8 column GROUP BY fast path (group_utf8.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 14.

## Pass 2 (faster)

Q14: same as Q11 pattern.
