# Q14

```sql
SELECT SearchPhrase, COUNT(DISTINCT UserID) AS u FROM hits WHERE SearchPhrase <> '' GROUP BY SearchPhrase ORDER BY u DESC LIMIT 10;
```

## Fast path

`group_fused.rs` — collect `(phrase_hash, UserID)` for filtered rows; `agg_sort::distinct_count_per_hash_sorted`; top-K by distinct count; resolve phrase strings for winners only.

## 100M warm (Jun 2026)

Hot **~9.2s** — still gap vs ClickHouse **~0.78s**; next target for filter/agg tuning.
