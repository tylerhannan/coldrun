# Q7

```sql
SELECT MIN(EventDate), MAX(EventDate) FROM hits;
```

## Adversarial

MIN/MAX pair is fused; other multi-minmax shapes still fall back.

## This pass

Uses existing fast_agg global paths; document baseline.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 7.

## Pass 2 (faster)

Q7: fused MIN/MAX date — optimal.
