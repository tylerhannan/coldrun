# Q26

```sql
SELECT SearchPhrase FROM hits WHERE SearchPhrase <> '' ORDER BY SearchPhrase LIMIT 10;
```

## Adversarial

ORDER BY SearchPhrase sorts utf8 — no collation, byte order only.

## This pass

scan_fast ORDER BY column paths.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 26.
