# Q25

```sql
SELECT SearchPhrase FROM hits WHERE SearchPhrase <> '' ORDER BY EventTime LIMIT 10;
```

## Adversarial

Fast scan sorts indices — good; still O(k log k) on filtered k.

## This pass

scan_fast ORDER BY column paths.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 25.
