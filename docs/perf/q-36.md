# Q36

```sql
SELECT ClientIP, ClientIP - 1, ClientIP - 2, ClientIP - 3, COUNT(*) AS c FROM hits GROUP BY ClientIP, ClientIP - 1, ClientIP - 2, ClientIP - 3 ORDER BY c DESC LIMIT 10;
```

## Adversarial

4×int pack good; ORDER BY c still sorts strings after unpack.

## This pass

Int/int64 packed GROUP BY (group_int.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 36.

## Pass 2 (faster)

Q36: int4 COUNT — ~unique keys on demo, heap top-10.
