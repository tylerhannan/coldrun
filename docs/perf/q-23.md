# Q23

```sql
SELECT SearchPhrase, MIN(URL), MIN(Title), COUNT(*) AS c, COUNT(DISTINCT UserID) FROM hits WHERE Title LIKE '%Google%' AND URL NOT LIKE '%.google.%' AND SearchPhrase <> '' GROUP BY SearchPhrase ORDER BY c DESC LIMIT 10;
```

## Adversarial

Four aggs + distinct — no fused path; LIKE filters not combined with group.

## This pass

Utf8 column GROUP BY fast path (group_utf8.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 23.

## Pass 2 (faster)

Q23: multi MIN+COUNT — interpreter.
