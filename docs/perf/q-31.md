# Q31

```sql
SELECT SearchEngineID, ClientIP, COUNT(*) AS c, SUM(IsRefresh), AVG(ResolutionWidth) FROM hits WHERE SearchPhrase <> '' GROUP BY SearchEngineID, ClientIP ORDER BY c DESC LIMIT 10;
```

## Adversarial

Two int keys packed; SUM+AVG still generic AggState per group.

## This pass

Int/int64 packed GROUP BY (group_int.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 31.

## Pass 2 (faster)

Q31: int pair fused aggs.
