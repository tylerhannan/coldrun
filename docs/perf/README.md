# Per-query performance notes

One commit per ClickBench query (`Perf Q1` … `Perf Q43`), each with adversarial “what still hurts” analysis.

| Query | Doc |
|-------|-----|
| Q1–Q43 | [`q-01.md`](q-01.md) … [`q-43.md`](q-43.md) |

Regenerate commits (docs only, after the first code commit):

```bash
./scripts/perf-q-commits.sh
```
