# Q13

```sql
SELECT SearchPhrase, COUNT(*) AS c FROM hits WHERE SearchPhrase <> '' GROUP BY SearchPhrase ORDER BY c DESC LIMIT 10;
```

## Adversarial

SearchPhrase cardinality on real hits is brutal; LIMIT 10 after full hash.

## This pass

Utf8 column GROUP BY fast path (group_utf8.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 13.
