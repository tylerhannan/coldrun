# Q35

```sql
SELECT 1, URL, COUNT(*) AS c FROM hits GROUP BY 1, URL ORDER BY c DESC LIMIT 10;
```

## Adversarial

Constant 1 folded; still full URL groups — no URL hash shortcut.

## This pass

Utf8 column GROUP BY fast path (group_utf8.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 35.

## Pass 2 (faster)

Q35: URL COUNT — string hash 100k×.
