# Q4

```sql
SELECT AVG(UserID) FROM hits;
```

## Adversarial

Single-column AVG without SIMD; Kahan summation missing for huge sums.

## This pass

Uses existing fast_agg global paths; document baseline.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 4.

## Pass 2 (faster)

Q4: single AVG column — could SIMD.
