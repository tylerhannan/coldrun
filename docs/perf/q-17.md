# Q17

```sql
SELECT UserID, SearchPhrase, COUNT(*) FROM hits GROUP BY UserID, SearchPhrase ORDER BY COUNT(*) DESC LIMIT 10;
```

## Fast path

`group_fused.rs` — `user_phrase_topk_by_sort`: parallel collect `(UserID, phrase_hash)`, sort + run-length count, heap top-10; one pass to resolve phrase text for top keys.

## 100M warm (Jun 2026)

Hot **~3.2s** (was **~12s** hash map; ClickHouse **~1.5s**).
