# Q11

```sql
SELECT MobilePhoneModel, COUNT(DISTINCT UserID) AS u FROM hits WHERE MobilePhoneModel <> '' GROUP BY MobilePhoneModel ORDER BY u DESC LIMIT 10;
```

## Adversarial

Utf8 GROUP BY still allocates key string on first sight; arena/dedup would help.

## This pass

Utf8 column GROUP BY fast path (group_utf8.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 11.

## Pass 2 (faster)

Q11: utf8+distinct int64 per group — heap top-10.
