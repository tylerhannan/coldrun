# Q27

```sql
SELECT SearchPhrase FROM hits WHERE SearchPhrase <> '' ORDER BY EventTime, SearchPhrase LIMIT 10;
```

## Adversarial

Two-key sort fast path; EventTime+SearchPhrase could use radix on ts.

## This pass

scan_fast ORDER BY column paths.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 27.
