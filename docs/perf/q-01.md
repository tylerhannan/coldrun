# Q1

```sql
SELECT COUNT(*) FROM hits;
```

## Adversarial

Still O(n) row count; no zone skip when table is empty. On 10B rows metadata-only COUNT would win.

## This pass

Uses existing fast_agg global paths; document baseline.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 1.
