# Q9

```sql
SELECT RegionID, COUNT(DISTINCT UserID) AS u FROM hits GROUP BY RegionID ORDER BY u DESC LIMIT 10;
```

## Fast path

`group_direct.rs` — parallel sort on `(RegionID, UserID)`, run-length distinct count per region, heap top-10.

Replaces bool-mask + hash-set over all rows (100M stall).

## 100M warm (Jun 2026)

Hot **~1.9s** (ClickHouse **~0.43s**).
