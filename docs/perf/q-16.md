# Q16

```sql
SELECT UserID, COUNT(*) FROM hits GROUP BY UserID ORDER BY COUNT(*) DESC LIMIT 10;
```

## Adversarial

UserID high cardinality — int GROUP BY correct but sort dominates.

## This pass

Int/int64 packed GROUP BY (group_int.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 16.

## Pass 2 (faster)

Q16: Int64 COUNT map + top-K heap.
