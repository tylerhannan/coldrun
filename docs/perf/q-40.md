# Q40

```sql
SELECT TraficSourceID, SearchEngineID, AdvEngineID, CASE WHEN (SearchEngineID = 0 AND AdvEngineID = 0) THEN Referer ELSE '' END AS Src, URL AS Dst, COUNT(*) AS PageViews FROM hits WHERE CounterID = 62 AND EventDate >= '2013-07-01' AND EventDate <= '2013-07-31' AND IsRefresh = 0 GROUP BY TraficSourceID, SearchEngineID, AdvEngineID, Src, Dst ORDER BY PageViews DESC LIMIT 10 OFFSET 1000;
```

## Adversarial

CASE expression group keys — full interpreter, no fast path.

## This pass

Document OFFSET/CASE cost; no fast path yet.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 40.

## Pass 2 (faster)

Q40: CASE in GROUP BY — interpreter only.
