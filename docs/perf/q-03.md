# Q3

```sql
SELECT SUM(AdvEngineID), COUNT(*), AVG(ResolutionWidth) FROM hits;
```

## Adversarial

Multi-agg fast path fuses passes but AVG still float-formatted; decimal would match ClickHouse.

## This pass

Uses existing fast_agg global paths; document baseline.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 3.
