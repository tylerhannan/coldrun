# Q20

```sql
SELECT UserID FROM hits WHERE UserID = 435090932899640449;
```

## Adversarial

Equality scan is linear; sorted UserID + zone index would be O(log n).

## This pass

Linear equality scan without full mask+sort (scan_fast.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 20.

## Pass 2 (faster)

Q20: equality scan linear — index on UserID would be O(1).
