# Q18

```sql
SELECT UserID, SearchPhrase, COUNT(*) FROM hits GROUP BY UserID, SearchPhrase LIMIT 10;
```

## Adversarial

LIMIT without ORDER BY — still scans all groups to fill 10 rows.

## This pass

Int/int64 packed GROUP BY (group_int.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 18.

## Pass 2 (faster)

Q18: int+utf8 LIMIT without ORDER BY — still builds all groups.
