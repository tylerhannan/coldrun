# Q18

```sql
SELECT UserID, SearchPhrase, COUNT(*) FROM hits GROUP BY UserID, SearchPhrase LIMIT 10;
```

## Fast path

`group_fused.rs` — `user_phrase_first_groups`: single pass; track only the first **LIMIT** distinct `(UserID, phrase_hash)` groups in scan order, count rows for those keys only.

No ORDER BY — must not build a map of all groups.

## 100M warm (Jun 2026)

Hot **~1.2s** (was **~21s** full hash agg; ClickHouse **~0.83s**).
