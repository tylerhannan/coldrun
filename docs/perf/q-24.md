# Q24

```sql
SELECT * FROM hits WHERE URL LIKE '%google%' ORDER BY EventTime LIMIT 10;
```

## Adversarial

SELECT * materializes all columns — worst query for columnar engine.

## This pass

No code change this pass — adversarial notes only.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 24.

## Pass 2 (faster)

Q24: index sort SELECT * — still materializes all cols.
