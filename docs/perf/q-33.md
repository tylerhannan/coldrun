# Q33

```sql
SELECT WatchID, ClientIP, COUNT(*) AS c, SUM(IsRefresh), AVG(ResolutionWidth) FROM hits GROUP BY WatchID, ClientIP ORDER BY c DESC LIMIT 10;
```

## Adversarial

Same as Q32 without SearchPhrase filter — even more groups.

## This pass

Int/int64 packed GROUP BY (group_int.rs).

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 33.

## Pass 2 (faster)

Q33: same kernel as Q32.
