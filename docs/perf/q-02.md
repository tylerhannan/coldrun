# Q2

```sql
SELECT COUNT(*) FROM hits WHERE AdvEngineID <> 0;
```

## Adversarial

Vectorized <> mask touches every row; bitmap/RLE would skip cold runs.

## This pass

Uses existing fast_agg global paths; document baseline.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 2.

## Pass 2 (faster)

Q2: nonzero int16 count without building bool mask.
