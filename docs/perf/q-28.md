# Q28

```sql
SELECT CounterID, AVG(length(URL)) AS l, COUNT(*) AS c FROM hits WHERE URL <> '' GROUP BY CounterID HAVING COUNT(*) > 100000 ORDER BY l DESC LIMIT 25;
```

## Adversarial

HAVING shortcut empty on demo; real data needs CounterID group + filter.

## This pass

HAVING shortcut + referer host GROUP BY.

## Still slow on demo @100k

Run `./scripts/bench-all.sh 100000` and compare query 28.
